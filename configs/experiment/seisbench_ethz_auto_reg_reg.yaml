# @package _global_

defaults:
  - /trainer: default
  - /loader: default
  - /dataset: seisbench_auto_reg
  - /task: regression
  - /optimizer: adamw_mamba
  - /scheduler: cosine_warmup
  - /model: mamba_sashimi

experiment_name: mamba_seisbench_auto_reg_regression

model:
  d_model: 64
  n_layers: 5
  expand: 2
  ff: 2
  pool:
    - 4
    - 4
  dropout: 0.0
  unet: False
  complex: True

train:
  monitor: val/loss
  mode: min

task:
  metrics:
    - log_mse
    - mse

dataset:
  sample_len: 1024
  bits: 0
  d_data: 3

encoder:
  _name_: linear

decoder:
  _name_: linear

loader:
  batch_size: 32

scheduler:
  num_warmup_steps: 100
  num_training_steps: 10000

trainer:
  max_steps: 10000
  max_epochs: 10000
  gradient_clip_val: 1.0
